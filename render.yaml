# render.yaml
services:
  - type: web
    name: telegram-anti-multi
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api/index.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
    autoDeploy: true